using System.Collections;
using System.Collections.Generic;
using UnityEngine;

using Key = Input.Key;

class Root : Player_Controller
{
    public void Input_Handle(Input input, Key key)
    {
        switch (key)
        {
            case Key.NONE:

                break;

            case Key.A:
            case Key.D:

                break;

            case Key.S:
            case Key.W:

                break;
        }
    }
}
    
