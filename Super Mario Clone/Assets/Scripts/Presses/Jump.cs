using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Presses
{
    public class Jump : Button
    {
        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}
